{
  "name": "mealendar",
  "version": "0.1.0",
  "private": true,
  "description": "Application de planification de repas multi-utilisateurs avec generation LLM",
  "packageManager": "pnpm@10.27.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "dev:api": "pnpm --filter @mealendar/api dev",
    "dev:mobile": "pnpm --filter @mealendar/mobile start",
    "build:android": "pnpm --filter @mealendar/mobile build:android:dev",
    "build:android:dev": "pnpm --filter @mealendar/mobile build:android:dev",
    "build:android:preview": "pnpm --filter @mealendar/mobile build:android:preview",
    "build:android:prod": "pnpm --filter @mealendar/mobile build:android:production",
    "build:ios": "pnpm --filter @mealendar/mobile build:ios:altstore",
    "build:ios:dev": "pnpm --filter @mealendar/mobile build:ios:dev",
    "build:ios:preview": "pnpm --filter @mealendar/mobile build:ios:preview",
    "build:ios:prod": "pnpm --filter @mealendar/mobile build:ios:production",
    "build:list": "pnpm --filter @mealendar/mobile build:list",
    "db:push": "node scripts/db-push.mjs",
    "db:status": "node scripts/db-push.mjs --dry-run",
    "db:reset": "node scripts/db-push.mjs --reset",
    "db:wipe": "node scripts/db-wipe.mjs",
    "db:inspect": "node scripts/db-inspect.mjs",
    "db:test-insert": "node scripts/db-test-insert.mjs",
    "db:list-users": "node scripts/db-list-users.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "postgres": "3.4.9",
    "typescript": "5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@biomejs/biome", "esbuild", "workerd"],
    "peerDependencyRules": {
      "ignoreMissing": ["react-dom"],
      "allowedVersions": {
        "react-dom>react": "19"
      }
    }
  }
}
